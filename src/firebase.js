import firebase from "firebase";

const config = {
  apiKey: "#############################",
  authDomain: "#############################",
  databaseURL: "#############################",
  projectId: "#############################",
  storageBucket: "#################kjafagflafhaskf;sa############",
  messagingSenderId: "#############################",
  appId: "#############################",
};

firebase.initializeApp(config);
export default firebase;
